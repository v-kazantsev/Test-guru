<% @page_title = "Edit" + question_header(@question.test) %>
<%= form_with model: @question, local: true do |f| %>
  <%= render 'shared/errors', resource: @question %>
  <p>
    <%= f.label :"Вопрос" %><br>
    <%= f.text_area :body %>
  </p>
  <input type="submit" value="Редактировать">
<% end %>
